import React from 'react'

interface Props {
    plan: string
    aboutPlan: string
    title: string
    features: string[]
    button: string
}

export default function Card(card: Props) {
    return (
        <div>
            
        </div>
    )
}
